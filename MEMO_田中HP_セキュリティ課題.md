# 田中HP セキュリティ・個人情報 課題メモ
> SHIBANOプロジェクト作業中（2026-05-04）に発覚した問題。田中HPで先に解決すべき事項。

---

## 課題①：市民相談CRMの個人情報が平文でD1に保存されている

田中HPの `workers/api.js` では、市民相談（consultations）の以下フィールドが**暗号化なしでD1に保存**されている。

- `citizen_name`（氏名）
- `address`（住所）
- `phone`（電話番号）
- `email`（メールアドレス）
- `line_id`（LINE ID）

### 対策
Workers内でWeb Crypto API（AES-GCM）を使い、D1保存前に上記フィールドを暗号化する。
暗号化キーはCloudflare Secretsで管理（`ENCRYPT_KEY`）。
→ `nda_template.html`（このフォルダ内）も参照。

---

## 課題②：admin画面の認証が弱い

- `/admin/` のURLを知っていれば誰でもログイン画面に到達できる
- パスワード＝Bearer Tokenそのままで総当たり攻撃が可能

### 対策
**Cloudflare Access（Zero Trust）** を `/admin/*` に設定する。
- 無料（50ユーザーまで）
- 設定はCloudflareダッシュボードのみ、コード変更ゼロ
- 許可メール（i.onoue@gmail.com）以外はページすら表示されない
- 田中HP・SHIBANO・草加自民HP・青柳院HP、全プロジェクト一括管理可能

---

## 課題③：個人情報保護方針ページが未整備

技術的対策（暗号化・Access）だけでは個人情報保護法上の義務を果たせない。
HPに個人情報保護方針ページが必要。

記載事項：
- 収集する情報の種類
- 利用目的（市民相談の対応・記録のため）
- 管理者（田中後援会事務所）
- 業務委託先（AntigravityWORKS）への情報共有の明示
- 開示・訂正・削除の請求方法
- 問い合わせ先

---

## 課題④：開発者（小野上）との秘密保持契約（NDA）が未締結

「第三者提供しない」と方針に書いても、開発者はD1・Workersにアクセスできる。
法律上は「業務委託先」として扱えばOKだが、**NDA（秘密保持契約）の締結が必要**。

→ NDA雛形は `nda_template.html`（このフォルダ内）に作成済み。
　田中HP分も同じ雛形で締結すること。

---

## 優先順位

1. Cloudflare Access 設定（コード変更なし・最優先）
2. NDA締結（印刷・署名するだけ）
3. 個人情報保護方針ページ作成
4. 暗号化実装（技術作業・最後）
